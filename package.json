{
  "name": "cool-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:server": "node server",
    "build": "vite build",
    "preview": "vite preview",
    "ssr:build": "npm run ssr:build:client && npm run ssr:build:server",
    "ssr:build:client": "vite build --ssrManifest --outDir dist/client",
    "ssr:build:server": "vite build --ssr server.js --outDir dist/server"
  },
  "devDependencies": {
    "vite": "^5.1.4"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cross-env": "^7.0.3",
    "express": "^4.18.3",
    "sirv": "^2.0.4"
  }
}
